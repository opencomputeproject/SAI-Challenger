name: sc-client-deb12-publish

on:
  push:
    branches:
      - 'main'
    paths:
      - '.github/workflows/sc-client-deb12-publish.yml'
      - 'dockerfiles/bookworm/Dockerfile.client'
      - 'dockerfiles/bookworm/Dockerfile.saithrift-client'
      - 'common/**'
      - 'cli/**'
      - 'scripts/**'
      - 'configs/**'
      - 'setup.py'
      - 'build.sh'
      - '.dockerignore'
      - 'sai.env'

jobs:
  build-sc-client:
    name: Build SAI Challenger client image
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v3
    - name: Update submodules
      run: git submodule update --init
    - name: Build client Docker image
      run: ./build.sh -i client -o deb12 --nosnappi
    - name: Build client Docker image with SAI thrift
      run: ./build.sh -i client -s thrift -o deb12 --nosnappi
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: plvisiondevs
        password: ${{ secrets.DOCKER_TOKEN }}
    - name: Create a tag for DockerHub
      run: docker tag sc-client:bookworm plvisiondevs/sc-client:bookworm-latest
    - name: docker push sc-client
      run: docker push plvisiondevs/sc-client:bookworm-latest
    - name: Create a tag for DockerHub
      run: docker tag sc-thrift-client:bookworm plvisiondevs/sc-thrift-client:bookworm-latest
    - name: docker push sc-thrift-client
      run: docker push plvisiondevs/sc-thrift-client:bookworm-latest
